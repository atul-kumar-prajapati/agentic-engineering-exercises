# Exercise Evaluation Rubrics

## Common 100-Point Structure

| Area | Points | Observable basis |
|---|---:|---|
| Correctness and working behavior | 30 | Required behavior works and seeded defects are addressed. |
| Requirement coverage | 25 | Every acceptance criterion is represented. |
| Verification and evidence | 20 | Commands and artifacts independently prove the claims. |
| Isolation and scope control | 15 | Changes stay within ownership boundaries and avoid unrelated work. |
| Reasoning and maintainability | 10 | Assumptions, trade-offs, and decisions are concise and traceable. |

A pass is 80 or more with every exercise-specific non-negotiable satisfied. A partial submission is 50 to 79 or has one remediable evidence gap. A fail is below 50, cannot run from a clean checkout, or misses a non-negotiable. Documentation volume earns no points unless it supplies required evidence.

## Agent Onboarding Kit

Non-negotiables: the fresh agent finds both routing-policy locations, changes only owned paths, keeps representative data valid, and records passing checks.

## Agent Guardrails

Non-negotiables: secret and production paths are blocked, destructive commands cannot execute, approval gates are distinguishable from blocks, bypass probes fail, and blocked attempts are audited.

## Spec-Driven Feature Development

Non-negotiables: three to five clarification questions precede the specification; authorization, billing, failure, and scope assumptions have consequences; spec, plan, and tasks are traceable.

## Playwright Checkout Rescue

Non-negotiables: the checkout remains reachable; fixed waits and generated-class selectors are removed from final coverage; payment state is isolated; payloads are asserted; the repeated parallel run passes with traceable evidence.

## MSW Network Boundaries

Non-negotiables: loading, success, server-empty, filtered-empty, error, and retry are distinct; unhandled requests fail; handlers reset; tests use the real `GET /api/cases` boundary.

## Pact Workflow Contract Gate

Non-negotiables: list and decision interactions are generated by the real client, provider states are named, and one clean command verifies the Pact against the Spring provider.

## Release Notes Skill Factory

Non-negotiables: notes use the real Git comparison, every published item is traceable, breaking changes and missing evidence are explicit, and internal-only work is excluded.

## jscodeshift Migration Playbook

Non-negotiables: the real class component is transformed, behavior is preserved, the stop fixture and unrelated component remain unchanged, and a second run produces no diff.

## Skill Trigger Eval Harness

Non-negotiables: the catalog is generated from real `SKILL.md` metadata, compound and ambiguous requests can abstain for human review, and both deterministic and sampled-model results report precision, recall, and confusion cases.

## Parallel Worktree Feature Split

Non-negotiables: all three product lanes have inspectable commits and handoffs, the controlled shared-file conflict is resolved by the integration owner, and lane and final checks pass.

## Specialist NFR Review

Non-negotiables: reports cite the final integrated SHA, accessibility and performance findings carry required evidence, the server policy boundary is reviewed, and affected specialists rerun after fixes.

## Agent-Ready Kanban

Non-negotiables: every card satisfies the schema, conflicting work is blocked or serialized, a failed/rejected/cancelled lane is explained, and board, ownership, app data, and integration log agree.

## Security and Accessibility Review Gauntlet

Non-negotiables: the real patch applies, scanner true and false positives are distinguished, the manual behavior bug is found, the server boundary is protected, and confirmed issues receive regression tests.

## Fresh-Agent Diff Triage

Non-negotiables: the exact bundled range is reviewed, real blockers are evidenced, the seeded unsupported claim is explicitly confirmed or dismissed, and blocker fixes pass verification.

## Review Regression Lab

Non-negotiables: the real review model is sampled, multi-bug recall and clean-control precision meet thresholds, the provider does not encode answers, and model/configuration/sample count/limitations are reported.
