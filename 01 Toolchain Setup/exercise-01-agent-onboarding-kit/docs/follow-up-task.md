# Fresh-Agent Follow-up Task

Run this only after the onboarding files have been submitted. Start a fresh agent with no conversation history. It may read only this repository and the submitted onboarding guidance.

Change the stale-case threshold from 18 hours to 24 hours. The rule exists in two policy locations. Update both, then update a representative sample case so the UI still demonstrates a stale, revenue-critical case. Do not edit unrelated UI or generated files.

The fresh agent must run the checks required by its onboarding guidance and record its result in `evidence/fresh-agent-result.md` using the supplied template.

Success means both policy locations agree, the representative case remains at or above the new threshold, the required checks pass, and the patch stays within the routing policy, sample data, and evidence file.
